extends layout 

block content 
    a(href='/inventory' class='return-btn') Back Home
    h1= title
    ul(id='section-list')
        each section in section_list 
            li(class='section-list-item')
                a(class='section-list-btn' href=section.url) #{section.name}
        else
            li There are no sections.